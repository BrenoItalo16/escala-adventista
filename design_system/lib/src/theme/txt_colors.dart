import 'dart:ui';

import 'package:design_system/src/theme/app_colors.dart';

class TxtColors {
  static const primary = Color(0xFF222222);
  static const support = Color(0xFF939AA5);
  static const hint = Color(0xFFC7C7C7);
  static const link = Color(0xFF00A3FF);
  static const error = AppColors.error;
  static const white = AppColors.white;
}
